import {createConnection, getConnection, getRepository} from "typeorm";

const mock1 = {}

describe("", () => {
    // beforeEach(async () => {
    //     await createConnection({
    //         type: "sqlite",
    //         database: ":memory:",
    //         dropSchema: true,
    //         entities: [crypto],
    //         synchronize: true,
    //         logging: false
    //     });
    // });

    // afterEach(async () => {
    //     const conn = getConnection();
    //     await conn.close();
    // });
    // test("", async () => {
    //     await getRepository(crypto).insert(mock1);
    //     const entities1 = await getRepository(crypto).find({});
    // })
});