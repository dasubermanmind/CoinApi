import { getRepository, Repository } from 'typeorm';
import { Request, Response } from 'express';

export default class AnalysisController {
  // Get the most recent analysis
  // Update most recent analysis
  // create a new analysis
}
